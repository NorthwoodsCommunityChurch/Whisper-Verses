import SwiftUI

/// Legacy update banner - now handled by Sparkle framework
/// This view is kept as an empty placeholder for backwards compatibility
struct UpdateBannerView: View {
    var body: some View {
        EmptyView()
    }
}
